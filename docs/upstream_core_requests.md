# Upstream Core Requests

No upstream Streamlit/core changes are requested yet.

When the Windows App needs a core API that is missing or unstable, document it here instead of editing the Streamlit repo directly.
